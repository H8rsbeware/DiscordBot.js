module.exports = config = ({
        consumer_key: process.env.twitterCK,
        consumer_secret: process.env.twitterCS,
        access_token: process.env.twitterATK,
        access_token_secret: process.env.twitterATS,
});
